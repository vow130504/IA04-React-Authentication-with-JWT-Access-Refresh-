# NestJS server port (local only; Render injects PORT at runtime)
PORT=3000

# MongoDB connection string (pick one, comment others)

# 1) Local MongoDB without auth
MONGODB_URI=mongodb://localhost:27017/user_registration_db

# 2) Local MongoDB with auth
# MONGODB_URI=mongodb://myuser:mypassword@localhost:27017/user_registration_db?authSource=admin

# 3) MongoDB Atlas example (recommended for Render)
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/user_registration_db?retryWrites=true&w=majority
# Lưu ý: Whitelist IP/outbound của Render hoặc mở 0.0.0.0/0 (không khuyến nghị)

# CORS: include your frontend origin(s). For GitHub Pages, origin is just https://<username>.github.io
# If your site is at https://<username>.github.io/<repo>, origin vẫn là https://<username>.github.io
FRONTEND_ORIGIN=http://localhost:5173,https://vow130504.github.io

# Bcrypt salt rounds for hashing passwords
SALT_ROUNDS=10

# Production hint
# NODE_ENV=production
